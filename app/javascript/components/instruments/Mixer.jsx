import React, { PureComponent } from 'react'

export default class Mixer extends PureComponent {
  constructor(props) {
    super(props)
  }

  render() {
    return <div className="Mixer">Mixer</div>
  }
}
